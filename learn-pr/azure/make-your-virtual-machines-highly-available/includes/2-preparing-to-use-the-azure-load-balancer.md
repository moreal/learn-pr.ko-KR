<span data-ttu-id="d3ab1-101">회사에서 Azure Load Balancer가 ERP(엔터프라이즈 리소스 계획) 응용 프로그램을 지원하는지 확인하고 싶어 한다고 가정해 봅시다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-101">Suppose your company wants to see if the Azure Load Balancer will support your Enterprise Resource Planning (ERP) application.</span></span> <span data-ttu-id="d3ab1-102">응용 프로그램은 사용자를 위한 웹 인터페이스를 제공하며 여러 서버에서 실행됩니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-102">Your application has a web interface for users and runs on multiple servers.</span></span> <span data-ttu-id="d3ab1-103">각 서버에는 모든 서버에서 동기화되는 ERP 데이터베이스의 로컬 복사본이 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-103">Each server has a local copy of the ERP database which is synced across all servers.</span></span>

<span data-ttu-id="d3ab1-104">여기서는 부하 분산 장치가 서비스 고가용성을 제공하는 원리를 살펴보겠습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-104">Here, you will look at how a load balancer can help provide high availability of services.</span></span> <span data-ttu-id="d3ab1-105">기본 부하 분산 장치와 표준 부하 분산 장치 옵션 간의 차이점을 살펴보고, Azure Virtual Machines에 사용할 부하 분산 장치를 만드는 방법을 알아보겠습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-105">You will identify the difference between the basic and standard load balancer options and see how to create a load balancer for Azure Virtual Machines.</span></span>

## <a name="what-is-load-balancing"></a><span data-ttu-id="d3ab1-106">부하 분산이란?</span><span class="sxs-lookup"><span data-stu-id="d3ab1-106">What is load balancing?</span></span>

<span data-ttu-id="d3ab1-107">_부하 분산_은 계산, 저장소, 네트워킹 등의 워크로드를 여러 장치에 분산하는 다양한 기술을 설명합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-107">_Load balancing_ describes various techniques for distributing workloads across multiple devices, such as compute, storage, and networking.</span></span> <span data-ttu-id="d3ab1-108">부하 분산의 목표는 여러 리소스를 최적화하고, 인프라 규모가 확장될 때 이러한 리소스를 최대한 효율적으로 사용하고, 일부 구성 요소를 사용할 수 없게 되더라도 서비스를 계속 유지하는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-108">The goal of load balancing is to optimize the use of multiple resources, to make the most efficient use these resources as an infrastructure is scaled out, and to ensure services are maintained if some components are unavailable.</span></span>

<span data-ttu-id="d3ab1-109">여기서는 VM(가상 머신)에 대한 Azure의 부하 분산 지원을 살펴보겠습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-109">Here, will look at Azure's load balancing support for virtual machines (VMs).</span></span>

### <a name="what-is-high-availability"></a><span data-ttu-id="d3ab1-110">고가용성이란?</span><span class="sxs-lookup"><span data-stu-id="d3ab1-110">What is High Availability?</span></span>

<span data-ttu-id="d3ab1-111">HA(고가용성)란 시스템 구성 요소에서 오류가 발생하더라도 응용 프로그램 또는 서비스에 계속 액세스할 수 있도록 유지하는 능력을 말합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-111">High availability (HA) measures the ability of an application or service to remain accessible despite a failure in any system component.</span></span> <span data-ttu-id="d3ab1-112">눈에 띄는 서비스 손실이 없는 것이 가장 이상적입니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-112">Ideally, there will be not be any noticeable loss of service.</span></span>

<span data-ttu-id="d3ab1-113">여러 VM이 서버 풀로 작동할 수 있게 해주는 부하 분산은 HA 제공을 위한 필수 요소입니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-113">Load balancing is fundamental to the delivery of HA because it allows multiple VMs to act as a pool of servers.</span></span> <span data-ttu-id="d3ab1-114">일부 VM에서 크래시가 발생하거나 유지 관리를 위해 오프라인으로 전환되더라도 풀은 계속해서 요청을 서비스할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-114">The pool can continue to service requests even if some VMs crash or are taken offline for maintenance.</span></span>

## <a name="what-is-the-azure-load-balancer"></a><span data-ttu-id="d3ab1-115">Azure Load Balancer란?</span><span class="sxs-lookup"><span data-stu-id="d3ab1-115">What is the Azure Load Balancer?</span></span>

<span data-ttu-id="d3ab1-116">**Azure Load Balancer**는 들어오는 요청을 풀의 여러 VM에 분산하는 Azure 서비스입니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-116">The **Azure Load Balancer** is an Azure service that distributes incoming requests across multiple VMs in a pool.</span></span> <span data-ttu-id="d3ab1-117">요청에 응답할 수 없는 VM이 발생하지 않도록, 들어오는 네트워크 트래픽을 정상적인 VM 집합에 분산합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-117">It distributes incoming network traffic across a set of healthy VMs and avoids any VM that is not able to respond.</span></span>

<span data-ttu-id="d3ab1-118">Azure Load Balancer는 OSI 7-레이어 모델의 레이어-4(TCP, UDP)에서 작동합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-118">The Azure Load Balancer operates at Layer-4 (TCP, UDP) of the OSI 7-layer model.</span></span> <span data-ttu-id="d3ab1-119">트래픽이 Azure VM으로 인바운드되는 TCP 및 UDP 응용 프로그램 시나리오, 그리고 다른 Azure 서비스가 Azure VM을 통해 TCP 및 UDP 트래픽을 외부 엔드포인트로 전달하는 아웃바운드 시나리오를 지원하도록 구성할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-119">It can be configured to support TCP and UDP application scenarios where the traffic is inbound to Azure VMs, as well as outbound scenarios where other Azure services are passing TCP and UDP traffic out through Azure VMs to external endpoints.</span></span>

## <a name="public-vs-internal-load-balancers"></a><span data-ttu-id="d3ab1-120">공용 부하 분산 장치와 내부용 부하 분산 장치의 차이점</span><span class="sxs-lookup"><span data-stu-id="d3ab1-120">Public vs. internal load balancers</span></span>

<span data-ttu-id="d3ab1-121">Azure Load Balancer는 들어오는 요청의 소스에 따라 _공용_일 수도 있고 _내부용_일 수도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-121">An Azure Load Balancer can be either _public_ or _internal_ depending on the source of incoming requests.</span></span>

<span data-ttu-id="d3ab1-122">**공용 부하 분산 장치**는 Azure 인프라 외부의 클라이언트 요청을 처리합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-122">A **public load balancer** handles client requests from outside of your Azure infrastructure.</span></span> <span data-ttu-id="d3ab1-123">공용 IP 및 분산 장치 리소스를 만들 때 부하 분산 장치의 공용 IP 주소는 자동으로 부하 분산 장치의 프런트 엔드로 구성됩니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-123">The public IP address of the load balancer is automatically configured as the load balancer's front end when you create the public IP and the load balancer resource.</span></span> <span data-ttu-id="d3ab1-124">다음 일러스트레이션은 공용 부하 분산 장치를 보여줍니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-124">The following illustration shows a public load balancer.</span></span>

![인터넷의 클라이언트 요청을 가상 네트워크의 세 VM으로 분산하는 공용 부하 분산 장치를 보여주는 일러스트레이션.](../media-draft/2-public-load-balancer.png)

<span data-ttu-id="d3ab1-126">**내부 부하 분산 장치**는 가상 네트워크 내에서(또는 VPN을 통해) 요청을 처리합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-126">An **internal load balancer** processes requests from within a virtual network (or through a VPN).</span></span> <span data-ttu-id="d3ab1-127">요청을 해당 vNet 내의 리소스에 분산합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-127">It distributes requests to resources within that vNet.</span></span> <span data-ttu-id="d3ab1-128">부하 분산 장치, 프런트 엔드 IP 주소 및 가상 네트워크는 인터넷에서 직접 액세스할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-128">The load balancer, frontend IP addresses, and virtual networks are not directly accessible from the internet.</span></span> <span data-ttu-id="d3ab1-129">다음 일러스트레이션은 공용 부하 분산 장치와 내부 부하 분산 장치를 모두 포함하는 아키텍처를 보여줍니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-129">The following illustration shows an architecture containing both a public and internal load balancer.</span></span> <span data-ttu-id="d3ab1-130">공용 부하 분산 장치는 외부 요청을 처리하는 반면, 내부 부하 분산 장치는 요청을 내부 VM 및 데이터베이스로 전달하여 처리합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-130">The public load balancer handles external requests while the internal load balancer forwards the requests to the internal VMs and databases for processing.</span></span>

![클라이언트 요청을 내부 부하 분산 장치로 전달하는 공용 부하 분산 장치를 보여주는 일러스트레이션.](../media-draft/2-internal-load-balancer.png)

## <a name="how-does-the-azure-load-balancer-work"></a><span data-ttu-id="d3ab1-134">Azure Load Balancer의 작동 원리</span><span class="sxs-lookup"><span data-stu-id="d3ab1-134">How does the Azure Load Balancer work?</span></span>

<span data-ttu-id="d3ab1-135">Azure Load Balancer는 **규칙** 및 **상태 프로브**에 구성된 정보를 사용하여 Load Balancer의 **프런트 엔드**에서 수신하는 새 인바운드 트래픽을 **백 엔드 풀**의 VM 인스턴스에 분산하는 방법을 결정합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-135">The Azure Load Balancer uses information configured in **rules** and **health probes** to determine how new inbound traffic that is received on the Load Balancer's **frontend** is distributed to VM instances in a **backend pool**.</span></span>

### <a name="frontend"></a><span data-ttu-id="d3ab1-136">프런트 엔드</span><span class="sxs-lookup"><span data-stu-id="d3ab1-136">Frontend</span></span>

<span data-ttu-id="d3ab1-137">부하 분산 장치 프런트 엔드는 하나 이상의 공용 IP 주소를 포함하고 있는 IP 구성으로, 인터넷을 통해 부하 분산 장치 및 해당 응용 프로그램에 액세스할 수 있게 해줍니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-137">The load balancer frontend is an IP configuration, containing one or more public IP addresses, that enables access to the load balancer and its applications over the Internet.</span></span>

### <a name="backend-address-pool"></a><span data-ttu-id="d3ab1-138">백 엔드 주소 풀</span><span class="sxs-lookup"><span data-stu-id="d3ab1-138">Backend address pool</span></span>

<span data-ttu-id="d3ab1-139">가상 머신은 vNIC(가상 네트워크 인터페이스 카드)를 사용하여 부하 분산 장치에 연결합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-139">Virtual machines connect to a load balancer using their virtual network interface card (vNIC).</span></span> <span data-ttu-id="d3ab1-140">백 엔드 주소 풀에는 부하 분산 장치에 연결되는 vNIC의 IP 주소가 포함되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-140">The back-end address pool contains the IP addresses of the vNICs that are connected to the load balancer.</span></span> <span data-ttu-id="d3ab1-141">모든 VM을 한 가용성 집합에 배치하면 부하 분산 장치를 구성할 때 VM을 백 엔드 풀에 손쉽게 추가할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-141">If you place all your VMs in an availability set, you can use this to easily add your VMs to a backend pool when configuring the load balancer.</span></span>

### <a name="health-probe"></a><span data-ttu-id="d3ab1-142">상태 프로브</span><span class="sxs-lookup"><span data-stu-id="d3ab1-142">Health probe</span></span>

<span data-ttu-id="d3ab1-143">부하 분산 장치는 _상태 프로브_를 사용하여 서비스 요청을 처리할 수 있는 가상 머신을 확인합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-143">Load balancers use _health probes_ to determine which virtual machines can service requests.</span></span> <span data-ttu-id="d3ab1-144">부하 분산 장치는 사용 가능하고 정상 작동하는 VM에만 트래픽을 분산합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-144">The load balancer will only distribute traffic to VMs that are available and operational.</span></span> 

<span data-ttu-id="d3ab1-145">상태 프로브는 각 VM의 특정 포트를 모니터링합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-145">A health probe monitors specific ports on each VM.</span></span> <span data-ttu-id="d3ab1-146">"상태"에 해당하는 응답 유형을 정의할 수 있습니다. 예를 들어 웹 응용 프로그램에 `HTTP 200 Available` 응답을 요구할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-146">You can define what type of response corresponds to "health"; for example, you might require an `HTTP 200 Available` response from a web application.</span></span> <span data-ttu-id="d3ab1-147">기본적으로 VM은 15초 간격으로 두 차례 연속으로 실패하면 "사용할 수 없음"으로 표시됩니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-147">By default, a VM will be marked as "unavailable" after two consecutive failures at 15-second intervals.</span></span>

### <a name="load-balancer-rules"></a><span data-ttu-id="d3ab1-148">부하 분산 장치 규칙</span><span class="sxs-lookup"><span data-stu-id="d3ab1-148">Load balancer rules</span></span>

<span data-ttu-id="d3ab1-149">부하 분산 장치 _규칙_은 트래픽을 백 엔드 VM에 분산하는 방법을 정의합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-149">Load balancer _rules_ define how traffic is distributed to backend VMs.</span></span> <span data-ttu-id="d3ab1-150">백 엔드 풀의 정상 VM에 요청을 균등하게 배포하는 것이 목표입니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-150">The goal is to distribute request fairly across the healthy VMs in the backend pool.</span></span>

<span data-ttu-id="d3ab1-151">Azure Load Balancer는 해시 기반 알고리즘을 사용하여 인바운드 패킷의 헤더를 다시 작성합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-151">The Azure Load Balancer uses a hash-based algorithm to rewrite the headers of inbound packets.</span></span> <span data-ttu-id="d3ab1-152">기본적으로 Load Balancer는 다음으로 해시를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-152">By default, the Load Balancer creates a hash from:</span></span>

- <span data-ttu-id="d3ab1-153">원본 IP 주소</span><span class="sxs-lookup"><span data-stu-id="d3ab1-153">source IP address</span></span>
- <span data-ttu-id="d3ab1-154">원본 포트</span><span class="sxs-lookup"><span data-stu-id="d3ab1-154">source port</span></span>
- <span data-ttu-id="d3ab1-155">대상 IP 주소</span><span class="sxs-lookup"><span data-stu-id="d3ab1-155">destination IP address</span></span>
- <span data-ttu-id="d3ab1-156">대상 포트</span><span class="sxs-lookup"><span data-stu-id="d3ab1-156">destination port</span></span>
- <span data-ttu-id="d3ab1-157">IP 프로토콜 번호</span><span class="sxs-lookup"><span data-stu-id="d3ab1-157">IP protocol number</span></span>

<span data-ttu-id="d3ab1-158">이 메커니즘은 패킷 클라이언트 흐름 내의 모든 패킷이 동일한 백 엔드 VM 인스턴스로 전송되도록 보장합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-158">This mechanism ensures that all packets within a packet client flow are sent to the same backend VM instance.</span></span> <span data-ttu-id="d3ab1-159">클라이언트의 새 흐름은 임의로 할당된 다른 원본 포트를 사용하므로 해시가 변경되고, 부하 분산 장치가 이 흐름을 다른 백 엔드포인트로 보낼 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-159">A new flow from a client will use a different randomly allocated source port, so the hash will change, and the load balancer may send this flow to a different backend endpoint.</span></span>

## <a name="basic-vs-standard-load-balancer-skus"></a><span data-ttu-id="d3ab1-160">기본 및 표준 부하 분산 장치 SKU의 차이점</span><span class="sxs-lookup"><span data-stu-id="d3ab1-160">Basic vs. Standard load balancer SKUs</span></span>

<span data-ttu-id="d3ab1-161">Azure Load Balancer는 **기본** 및 **표준**의 두 종류가 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-161">There are two types of Azure Load Balancers: **Basic** and **Standard**.</span></span> <span data-ttu-id="d3ab1-162">둘은 규모, 기능 및 가격 측면에서 차이가 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-162">They differ in scale, features, and pricing.</span></span> <span data-ttu-id="d3ab1-163">예:</span><span class="sxs-lookup"><span data-stu-id="d3ab1-163">For example:</span></span>

- <span data-ttu-id="d3ab1-164">표준은 HTTPS를 지원하지만 기본은 지원하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-164">Standard supports HTTPS while Basic does not</span></span>
- <span data-ttu-id="d3ab1-165">표준의 풀 크기가 훨씬 큽니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-165">Pool size can be much larger in Standard</span></span>
- <span data-ttu-id="d3ab1-166">기본은 비용이 발생하지 않지만, 표준은 규칙 및 처리량에 따라 요금이 청구됩니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-166">Basic is no-cost while Standard is charged based on rules and throughput.</span></span>

<span data-ttu-id="d3ab1-167">표준은 기본의 상위 집합이므로 기본에 적합한 모든 시나리오는 표준에서도 작동합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-167">Standard is a superset of Basic, so any scenario suitable for Basic should also work on Standard.</span></span> <span data-ttu-id="d3ab1-168">기본 SKU는 일반적으로 프로토타입 및 테스트용으로 사용되고, 표준은 프로덕션 환경에 권장됩니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-168">The Basic SKU is generally intended for prototyping and testing while Standard is recommended for production.</span></span>

## <a name="start-the-deployment-of-a-basic-public-load-balancer"></a><span data-ttu-id="d3ab1-169">기본 공용 Load Balancer 배포 시작</span><span class="sxs-lookup"><span data-stu-id="d3ab1-169">Start the deployment of a Basic Public Load Balancer</span></span>

<span data-ttu-id="d3ab1-170">부하 분산 VM 시스템을 만들려면 부하 분산 장치 자체를 만들고, 가상 머신을 포함할 가상 네트워크를 만들고, 가상 네트워크에 VM을 추가해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-170">To create a load-balanced VM system, you need to create the load balancer itself, create a virtual network to contain your virtual machines, and then add VMs to the virtual network.</span></span>

<span data-ttu-id="d3ab1-171">Azure Portal을 사용하여 부하 분산 장치를 만들려면 다음 항목을 정의합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-171">To create the load balancer using the Azure portal, you define:</span></span>

- <span data-ttu-id="d3ab1-172">부하 분산 장치 이름</span><span class="sxs-lookup"><span data-stu-id="d3ab1-172">Load balancer name</span></span>
- <span data-ttu-id="d3ab1-173">유형: 공용 또는 내부용</span><span class="sxs-lookup"><span data-stu-id="d3ab1-173">Type: Public or Internal</span></span>
- <span data-ttu-id="d3ab1-174">SKU: 기본 또는 표준</span><span class="sxs-lookup"><span data-stu-id="d3ab1-174">SKU: Basic or Standard</span></span>
- <span data-ttu-id="d3ab1-175">공용 IP 주소: 동적 또는 고정</span><span class="sxs-lookup"><span data-stu-id="d3ab1-175">Public IP address: Dynamic or Static</span></span>
- <span data-ttu-id="d3ab1-176">리소스 그룹 및 위치</span><span class="sxs-lookup"><span data-stu-id="d3ab1-176">Resource group and Location</span></span>

<span data-ttu-id="d3ab1-177">백 엔드 VM은 모두 동일한 가상 네트워크(vNET)에 연결되므로, 그 다음으로 다음과 같은 리소스를 구성해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-177">Your backend VMs will all be connected to the same virtual network (vNET), so you next need to configure this resource:</span></span>

- <span data-ttu-id="d3ab1-178">vNET 이름</span><span class="sxs-lookup"><span data-stu-id="d3ab1-178">vNET name</span></span>
- <span data-ttu-id="d3ab1-179">사용할 주소 공간(예: 172.20.0.0/16)</span><span class="sxs-lookup"><span data-stu-id="d3ab1-179">Address space to use, such as 172.20.0.0/16</span></span>
- <span data-ttu-id="d3ab1-180">리소스 그룹</span><span class="sxs-lookup"><span data-stu-id="d3ab1-180">Resource group</span></span>
- <span data-ttu-id="d3ab1-181">사용할 서브넷의 이름</span><span class="sxs-lookup"><span data-stu-id="d3ab1-181">Name for the subnet to use</span></span>
- <span data-ttu-id="d3ab1-182">172.20.0.0/24와 같은 서브넷의 주소 공간(기본 공간 내에 포함되어야 함)</span><span class="sxs-lookup"><span data-stu-id="d3ab1-182">Address space for the subnet (must be within the main space), such as 172.20.0.0/24</span></span>

<span data-ttu-id="d3ab1-183">그 후에는 백 엔드 VM을 만들어서 배포하고, vNET을 사용하도록 구성해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-183">You then need to create and deploy your backend VMs and configure them to use your vNET.</span></span> <span data-ttu-id="d3ab1-184">또한 VM을 동일한 가용성 집합에 배치해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-184">You should also place your VMs into the same availability set.</span></span> <span data-ttu-id="d3ab1-185">가용성 집합은 VM 그룹의 내결함성 수준을 정의하지만, 부하 분산을 위해 VM을 백 엔드 풀에 할당하는 것을 도와줍니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-185">Availability sets define the level of fault tolerance across a group of VMs, but for load balancing, they also help you assign your VMs to backend pools.</span></span>

<span data-ttu-id="d3ab1-186">고가용성 솔루션의 일부로 Azure Load Balancer를 사용하는 방법을 살펴보았습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-186">You have now seen how to use an Azure Load Balancer as part of a high-availability solution.</span></span> <span data-ttu-id="d3ab1-187">다음으로, 다음 단계에 따라 부하 분산 장치를 배포하겠습니다.</span><span class="sxs-lookup"><span data-stu-id="d3ab1-187">Next, you will use these steps to deploy your own load balancer.</span></span>